# Angela An Portfolio

Static personal portfolio website for Angela An.

## Preview locally

Open `index.html` directly or run:

```bash
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.

## Resume PDF

Add the final resume file at:

```text
assets/Angela-An-Resume.pdf
```

The resume download buttons are already wired to that path.
